import { StyleSheet, View } from 'react-native';
import { RNContainer, RNText } from '../../Common';

export default function ResetPassword() {
  return (
    <RNContainer>
      <RNText>ResetPassword</RNText>
    </RNContainer>
  );
}

const styles = StyleSheet.create({});
