import { StyleSheet, View } from 'react-native';
import { RNContainer, RNText } from '../../Common';

export default function ForgotPassword({ navigation }) {
  return (
    <RNContainer>
      <RNText>ForgotPassword</RNText>
    </RNContainer>
  );
}

const styles = StyleSheet.create({});
